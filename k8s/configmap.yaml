apiVersion: v1
kind: ConfigMap
metadata:
  name: portal-b-config-staging
  namespace: exam-2
data:
  config.json: |
    {
      "app_name": "Portal",
      "subtitle": "Running on Kubernetes with GitOps - Argo CD",
      "owner": "anton
    }
